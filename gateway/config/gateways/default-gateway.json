{
  "apiVersion": "iip.gateway/v1alpha1",
  "kind": "Gateway",
  "metadata": {
    "name": "default-gateway"
  },
  "spec": {
    "logging": {
      "errorLog": {
        "level": "info"
      },
      "accessLog": {
        "enabled": true,
        "format": "JSON"
      }
    }
  }
}
